# Code generated by shipbuilder init 1.16.6. DO NOT EDIT.

name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: conventional-actions/setup-netrc@v1
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v3
        with:
          go-version: '^1.18'
      - uses: conventional-actions/winch@v1
      - uses: conventional-actions/go-kart@v0.0.10
      - uses: conventional-actions/go-vet@v0.0.1
      - uses: conventional-actions/go-revive@v0.0.5
      - uses: conventional-actions/go-staticcheck@v1.0.1
      - run: |
          ls -l *.sarif

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
  SNYK_AUTH_TOKEN: ${{ secrets.SNYK_AUTH_TOKEN }}
